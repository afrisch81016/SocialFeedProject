import "./NavBar.css"

const NavBar = (props) => {

    return (
        <table>
            <span className='socialspan'>Social</span><span className='feedspan'>Feed</span>
        </table>

    );
}

export default NavBar